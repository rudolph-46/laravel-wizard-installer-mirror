{
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@tailwindcss/cli": "^4.0.0"
  },
  "scripts": {
    "build": "npx @tailwindcss/cli -i resources/css/tailwind.css -o public/styles.css --minify"
  }
}
